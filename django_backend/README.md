# django_backend

This directory is a helper mirror for the project structure.

The active Django backend is located in `backend/` and the application code lives in the top-level `api/` package.

Use the project root `manage.py` and `backend.settings` to run the application:

```bash
python manage.py runserver
```
